suite: ServiceMonitor Snapshot Test
templates:
  - manifests.yaml
tests:
  - it: Full Configured (Snapshot)
    values:
      - ./values/manifests/serviceMonitor.yaml
    asserts:
      - matchSnapshot: {}