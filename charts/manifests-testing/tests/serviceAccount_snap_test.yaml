suite: Test ServiceAccount Snapshot
templates:
  - manifests.yaml
tests:
  - it: Full Configured (Snapshot)
    values:
      - ./values/manifests/serviceAccount.yaml
    asserts:
      - matchSnapshot: {}